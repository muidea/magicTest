# Namespace 测试用例分类整理

## 1. 基础CURD操作

### 1.1 创建操作
| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-001 | 正常 | 创建基本命名空间 | `name: "ns1", scope: "*"` | 创建成功 |
| N-TC-002 | 正常 | 创建多作用域命名空间 | `scope: "ns1,ns2,ns3"` | 创建成功 |
| N-TC-003 | 正常 | 创建空作用域命名空间 | `scope: ""` | 创建成功 |

### 1.2 读取操作
| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-011 | 正常 | 查询命名空间 | 有效命名空间ID | 返回完整信息 |
| N-TC-012 | 正常 | 过滤命名空间(按名称) | `name: "ns1"` | 返回匹配命名空间 |
| N-TC-013 | 正常 | 过滤命名空间(按状态) | `status: 2` | 返回启用状态的命名空间 |
| N-TC-014 | 正常 | 过滤命名空间(按作用域) | `scope: "*"` | 返回全局作用域命名空间 |

### 1.3 更新操作
| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-008 | 正常 | 更新命名空间描述 | `description: "新描述"` | 更新成功 |
| N-TC-009 | 正常 | 更新命名空间作用域 | `scope: "new1,new2"` | 更新成功 |
| N-TC-010 | 正常 | 更新命名空间状态 | `status: 1 (禁用)` | 状态变更成功 |

### 1.4 删除操作
| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-015 | 正常 | 删除命名空间 | 有效命名空间ID | 删除成功 |

## 2. 异常测试

### 2.1 创建异常
| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-006 | 异常 | 创建重复名称命名空间 | 已存在的命名空间名 | 创建失败 |
| N-TC-007 | 异常 | 创建无效时间命名空间 | `startTime > expireTime` | 创建失败 |

## 3. 边界测试

| 用例ID | 测试类型 | 描述 | 输入数据 | 预期结果 |
|--------|----------|------|----------|----------|
| N-TC-004 | 边界 | 创建超长名称命名空间 | `name: 256字符` | 创建成功或适当处理 |
| N-TC-005 | 边界 | 创建特殊字符作用域 | `scope: "a*b,c@d"` | 创建成功或验证处理 |

## 4. 功能测试场景

### 场景 N1: 命名空间作用域逻辑
- **前置条件**: 无
- **测试步骤**:
  1. 创建全局作用域 (`"*"`) 命名空间
  2. 创建跨空间访问 (`"n1,n2"`) 命名空间
  3. 创建仅限自身 (`""`) 命名空间
  4. 验证不同作用域的权限隔离
- **预期结果**: 作用域逻辑正确实现

### 场景 N2: 命名空间时间管理
- **前置条件**: 当前时间戳
- **测试步骤**:
  1. 创建有效时间范围的命名空间
  2. 创建已过期的命名空间
  3. 创建未开始的命名空间
  4. 验证时间相关的业务逻辑
- **预期结果**: 时间管理功能正常

### 场景 N3: 命名空间层级关系
- **前置条件**: 父命名空间
- **测试步骤**:
  1. 在父命名空间下创建子命名空间
  2. 验证层级关系
  3. 测试权限继承
  4. 测试跨层级访问
- **预期结果**: 层级关系正确处理

## 5. 测试统计

| 测试类型 | 用例数量 | 完成状态 |
|----------|----------|----------|
| 基础CURD | 11 | 待实现 |
| 异常测试 | 2 | 待实现 |
| 边界测试 | 2 | 待实现 |
| 功能场景 | 3 | 待实现 |
| **总计** | **18** | **待实现** |

## 6. 测试优先级

1. **P0 (高优先级)**: N-TC-001, N-TC-002, N-TC-011, N-TC-015 (基础功能)
2. **P1 (中优先级)**: N-TC-003, N-TC-008, N-TC-009, N-TC-010, N-TC-012, N-TC-013 (常用功能)
3. **P2 (低优先级)**: 所有异常和边界测试用例

## 7. 特殊注意事项

- **作用域逻辑**: 命名空间的作用域字段影响其他实体的访问权限
- **时间管理**: 命名空间的时间范围影响实体在该命名空间内的有效性
- **层级关系**: 命名空间可能支持父子层级关系，需要测试继承逻辑
- **全局作用域**: `"*"` 表示全局访问权限，需要特殊处理